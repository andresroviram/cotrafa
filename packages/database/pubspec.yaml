name: cotrafa_database
description: Cotrafa local Drift database package.
publish_to: none
resolution: workspace
version: 0.0.1

environment:
  sdk: ^3.12.2
  flutter: ">=3.44.9"

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../core
  drift: ^2.24.0
  injectable: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.15.0
  drift_dev: ^2.33.0
  injectable_generator: ^3.0.2

flutter:
  uses-material-design: true
