org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=true
# Kotlin 2.4.0's relocatable incremental cache still can't compute relative
# paths across Windows drive letters (e.g. project on D:, PUB_CACHE on C:).
# This remains necessary with Flutter 3.47 and AGP's built-in Kotlin support.
kotlin.incremental=false
