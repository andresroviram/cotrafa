org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Kotlin's relocatable incremental cache can't compute relative paths across
# Windows drive letters (e.g. project on D:, PUB_CACHE on C:), crashing
# compileDebugKotlin with "different roots". Disabling incremental compilation
# avoids that code path entirely, independent of where each dev's caches live.
kotlin.incremental=false
