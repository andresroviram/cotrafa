# Workspace scripts

| Script | Purpose |
| --- | --- |
| `setup_web.sh` / `setup_web.ps1` | Download `sqlite3.wasm` and compile the Drift web worker. |
| `check_coverage.sh` / `check_coverage.ps1` | Run workspace coverage and enforce the configured line threshold. |

Run scripts from the workspace root. Both script families require FVM.
